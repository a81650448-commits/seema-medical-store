(function(){'use strict';
const KEY='seema_medical_bills_v1';
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const money=v=>'₹'+Number(v||0).toFixed(2);
const getBills=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch(e){return[]}};
const saveBills=a=>localStorage.setItem(KEY,JSON.stringify(a));
const invoiceNo=()=>`BILL-${Date.now().toString().slice(-8)}`;
function words(n){n=Math.round(Number(n||0));if(!n)return'Zero Rupees Only';const a=['','One','Two','Three','Four','Five','Six','Seven','Eight','Nine','Ten','Eleven','Twelve','Thirteen','Fourteen','Fifteen','Sixteen','Seventeen','Eighteen','Nineteen'],b=['','','Twenty','Thirty','Forty','Fifty','Sixty','Seventy','Eighty','Ninety'];const f=x=>x<20?a[x]:b[Math.floor(x/10)]+(x%10?' '+a[x%10]:'');const h=x=>x<100?f(x):a[Math.floor(x/100)]+' Hundred'+(x%100?' '+f(x%100):'');let r='',cr=Math.floor(n/1e7);n%=1e7;let la=Math.floor(n/1e5);n%=1e5;let th=Math.floor(n/1e3);n%=1e3;if(cr)r+=h(cr)+' Crore ';if(la)r+=f(la)+' Lakh ';if(th)r+=f(th)+' Thousand ';if(n)r+=h(n);return r.trim()+' Rupees Only'}
function gstPrint(b){const items=Array.isArray(b.items)?b.items:[];const base=items.reduce((s,i)=>s+Number(i.price??i.rate||0)*Number(i.qty||0),0);const sg=base*.025,cg=base*.025,total=base+sg+cg;const rows=items.map((i,k)=>{const rate=Number(i.price??i.rate||0),qty=Number(i.qty||0);return `<tr><td>${k+1}.</td><td class=l>${esc(i.name||'Medicine')}</td><td>${esc(i.pack||'')}</td><td>${esc(i.hsn||'')}</td><td>${esc(i.batch||'')}</td><td>${esc(i.exp||'')}</td><td>${qty}</td><td>${money(i.mrp??rate)}</td><td>${money(rate)}</td><td>2.50</td><td>2.50</td><td>${money(rate*qty*1.05)}</td></tr>`}).join('');const date=new Date(b.date||Date.now()).toLocaleDateString('en-IN');const w=window.open('','_blank','width=1200,height=850');if(!w){alert('Please allow pop-ups for the GST bill.');return}w.document.write(`<!doctype html><html><head><meta charset=utf-8><title>${esc(b.invoice||'GST Invoice')}</title><style>@page{size:A4 landscape;margin:7mm}*{box-sizing:border-box}body{font-family:Arial,sans-serif;font-size:10px;color:#111;margin:0}.inv{border:1px solid #111}.top{display:grid;grid-template-columns:1.1fr 1fr;border-bottom:1px solid #111}.top>div{padding:7px 9px}.top>div+div{border-left:1px solid #111}.store{font-size:18px;font-weight:800}.addr{margin:4px 0 12px}.gst{padding:6px 9px;border-bottom:1px solid #111;font-weight:700}.title{position:relative;text-align:center;font-size:20px;font-weight:800;padding:7px;border-bottom:1px solid #111}.meta{position:absolute;right:9px;top:8px;font-size:10px;font-weight:400;text-align:right}.tbl{width:100%;border-collapse:collapse;table-layout:fixed}.tbl th,.tbl td{border-right:1px solid #111;border-bottom:1px solid #111;padding:4px 2px;text-align:center;word-wrap:break-word}.tbl th{background:#d9f4f4;font-size:9px}.tbl .l{text-align:left}.bottom{display:grid;grid-template-columns:1fr 260px}.notes{padding:7px 9px;min-height:155px}.tot{border-left:1px solid #111}.tot div{display:flex;justify-content:space-between;padding:5px 8px}.grand{margin-top:55px;border-top:1px solid #111;font-size:15px;font-weight:800;padding:9px 8px!important}.terms{margin-top:8px;font-weight:700}.terms p{font-weight:400;margin:3px 0}.sign{text-align:right;margin-top:10px}.amt{margin-top:8px;font-weight:700}@media print{.printnote{display:none}}</style></head><body><div class=inv><div class=top><div><div class=store>ADITYA MEDICAL STORE</div><div class=addr>CIVIL LINE LOHIA POOL KASIA KUSHINAGAR</div><b>Phone :</b> 8858562745<br><b>E-Mail :</b> shatrughanbhu@gamil.com</div><div><b>Patient Name :</b> ${esc(b.customer||'Walk-in Customer')}<br><b>Patient Address :</b> ${esc(b.address||'')}<br><b>Dr Name :</b> ${esc(b.doctor_name||'')}<br><b>Dr Reg No.</b> ${esc(b.doctor_reg_no||'')}</div></div><div class=gst>GSTIN : 09BGMPK4296F1ZY</div><div class=title>GST INVOICE<div class=meta>Invoice No. : ${esc(b.invoice||'')}<br>Date : ${date}</div></div><table class=tbl><thead><tr><th>SN.</th><th>PRODUCT NAME</th><th>PACK</th><th>HSN</th><th>BATCH</th><th>EXP.</th><th>QTY</th><th>MRP</th><th>RATE</th><th>SGST</th><th>CGST</th><th>AMOUNT</th></tr></thead><tbody>${rows}</tbody></table><div class=bottom><div class=notes><b>Scan &amp; Book</b><div style="margin:6px 0">▣ SCAN ME!</div><div class=terms><b>Terms &amp; Conditions</b><p>Goods once sold will not be taken back or exchanged.</p><p>Bills not paid due date will attract 24% interest.</p><p>All disputes subject to Jurisdication only.</p><p>Prescribed Sales Tax declaration will be given.</p><p>Remark :</p></div><div class=amt>Rs. ${words(total)}</div><div class=sign>For ADITYA MEDICAL STORE<br><br>Authorised Signatory</div></div><div class=tot><div><span>SUB TOTAL</span><b>${money(base)}</b></div><div><span>SGST 2.5 %</span><b>${money(sg)}</b></div><div><span>CGST 2.5 %</span><b>${money(cg)}</b></div><div class=grand><span>GRAND TOTAL</span><b>${money(total)}</b></div></div></div></div><script>window.onload=function(){window.print()}<\/script></body></html>`);w.document.close()}
async function meds(){const r=await window.supabase.from('medicines').select('*').order('name');return r.error?[]:(r.data||[])}
async function orders(){const r=await window.supabase.from('orders').select('*').order('created_at',{ascending:false});return r.error?[]:(r.data||[])}
function replace(){const s=document.getElementById('billing');if(!s||s.dataset.gstBilling==='1')return;s.dataset.gstBilling='1';s.innerHTML=`<h2>Billing & Invoices</h2><div class="bill-tabs"><button class="btn primary" data-btab="off">🧾 New Offline Sale</button><button class="btn" data-btab="on">📦 Online Order → Bill</button><button class="btn" data-btab="hist">📚 Bill History</button></div><div id="gstOff" class="card"></div><div id="gstOn" class="card hidden"></div><div id="gstHist" class="card hidden"></div>`;s.querySelectorAll('[data-btab]').forEach(x=>x.onclick=()=>{s.querySelectorAll('[data-btab]').forEach(y=>y.classList.remove('primary'));x.classList.add('primary');['off','on','hist'].forEach(k=>document.getElementById('gst'+({off:'Off',on:'On',hist:'Hist'}[k])).classList.toggle('hidden',k!==x.dataset.btab));if(x.dataset.btab==='on')online();if(x.dataset.btab==='hist')history()});offline();}
async function offline(){const p=document.getElementById('gstOff');if(!p)return;const m=await meds();p.innerHTML=`<h3>New Offline Sale</h3><div class="formgrid"><label>Customer Name<input id="gName" class="input" placeholder="Walk-in Customer"></label><label>Mobile<input id="gPhone" class="input" placeholder="Optional"></label><label>Payment<select id="gPay" class="select"><option>Cash</option><option>UPI</option><option>Card</option></select></label><label>Medicine<select id="gMed" class="select">${m.map(x=>`<option value="${x.id}">${esc(x.name)} — ${money(x.price)} — Stock ${x.stock}</option>`).join('')}</select></label></div><label>Quantity<input id="gQty" class="input" type="number" min="1" value="1"></label><br><button id="gAdd" class="btn primary">Add to Bill</button><div id="gCart"></div><button id="gSave" class="btn primary">Save Sale & Print Bill</button>`;window.gcart=[];function render(){document.getElementById('gCart').innerHTML=gcart.length?`<table class=table><thead><tr><th>Medicine</th><th>Qty</th><th>Rate</th><th>Amount</th></tr></thead><tbody>${gcart.map(x=>`<tr><td>${esc(x.name)}</td><td>${x.qty}</td><td>${money(x.price)}</td><td>${money(x.qty*x.price)}</td></tr>`).join('')}</tbody></table><h3 style="text-align:right">Total: ${money(gcart.reduce((a,x)=>a+x.qty*x.price,0))}</h3>`:'<p class=muted>No medicines added to this bill.</p>'}document.getElementById('gAdd').onclick=()=>{const x=m.find(z=>String(z.id)===document.getElementById('gMed').value),q=Math.max(1,Number(document.getElementById('gQty').value||1));if(!x||q>Number(x.stock||0)){alert('Insufficient stock.');return}gcart.push({id:x.id,name:x.name,price:Number(x.price||0),qty:q});render()};document.getElementById('gSave').onclick=async()=>{if(!gcart.length){alert('Add medicine first.');return}for(const x of gcart){const z=m.find(a=>String(a.id)===String(x.id));const r=await window.supabase.from('medicines').update({stock:Number(z.stock||0)-x.qty}).eq('id',x.id);if(r.error){alert(r.error.message);return}}const b={invoice:invoiceNo(),type:'Offline Sale',date:new Date().toISOString(),customer:document.getElementById('gName').value.trim()||'Walk-in Customer',phone:document.getElementById('gPhone').value.trim(),payment:document.getElementById('gPay').value,items:gcart,total:gcart.reduce((a,x)=>a+x.qty*x.price,0)};const a=getBills();a.unshift(b);saveBills(a);gstPrint(b);offline()};render()}
async function online(){const p=document.getElementById('gstOn'),os=await orders();p.innerHTML=`<h3>Online Order → Bill</h3><select id="gOrder" class="select"><option value="">Select order</option>${os.filter(x=>x.status!=='Cancelled').map(x=>`<option value="${x.id}">${esc(x.order_id)} — ${esc(x.customer_name)} — ${money(x.total)}</option>`).join('')}</select><br><button id="gOnline" class="btn primary">Create & Print Bill</button>`;document.getElementById('gOnline').onclick=()=>{const o=os.find(x=>String(x.id)===String(document.getElementById('gOrder').value));if(!o){alert('Select an order.');return}const b={invoice:invoiceNo(),type:'Online Order',date:new Date().toISOString(),customer:o.customer_name,phone:o.phone||'',address:o.address||'',payment:o.payment_method||'COD',items:(o.items||[]).map(i=>({name:i.name,qty:Number(i.qty||0),price:Number(i.price||0),pack:i.pack,hsn:i.hsn,batch:i.batch,exp:i.exp,mrp:i.mrp})),total:Number(o.total||0),orderId:o.order_id};const a=getBills();a.unshift(b);saveBills(a);gstPrint(b)}}
function history(){const p=document.getElementById('gstHist'),a=getBills();p.innerHTML='<h3>Bill History</h3>'+ (a.length?`<table class=table><thead><tr><th>Invoice</th><th>Customer</th><th>Type</th><th>Amount</th><th>Action</th></tr></thead><tbody>${a.map(b=>`<tr><td>${esc(b.invoice)}</td><td>${esc(b.customer)}</td><td>${esc(b.type)}</td><td>${money(b.total)}</td><td><button class="btn primary" data-p="${esc(b.invoice)}">Print</button></td></tr>`).join('')}</tbody></table>`:'<p>No bills found.</p>');p.querySelectorAll('[data-p]').forEach(x=>x.onclick=()=>{const b=a.find(z=>z.invoice===x.dataset.p);if(b)gstPrint(b)})}
function start(){replace();new MutationObserver(replace).observe(document.body,{childList:true,subtree:true})}window.addEventListener('load',start);setTimeout(start,500);})();